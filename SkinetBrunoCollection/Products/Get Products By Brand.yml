info:
  name: Get Products By Brand
  type: http
  seq: 8

http:
  method: GET
  url: "{{host}}/product?brand=React"
  params:
    - name: brand
      value: React
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
