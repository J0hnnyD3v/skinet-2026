info:
  name: Get product by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{host}}/product/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
